interface:
  display_name: "Common Git Commit"
  short_description: "Create one or more clean Git commits from the current diff."
  default_prompt: "Analyze the current branch changes, split them into logical commits when needed, and create the commit(s) without pushing."
